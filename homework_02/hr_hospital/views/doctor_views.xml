<odoo>
    <record id="view_doctor_form" model="ir.ui.view">
        <field name="name">hr_hospital.doctor.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form/sheet" position="inside">
                <field name="is_doctor" invisible="0"/>
                <field name="specialty"/>
                <notebook>
                        <page string="Visits">
                            <field name="visit_ids" context="{'default_doctor_id': active_id}" widget="one2many_list"/>
                        </page>
                </notebook>
            </xpath>
        </field>
    </record>

    <record id="view_doctor_tree" model="ir.ui.view">
        <field name="name">hr_hospital.doctor.tree</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="inside">
                <field name="specialty"/>
                <field name="is_doctor" invisible="0"/>
            </xpath>
        </field>
    </record>

    <record id="action_doctor" model="ir.actions.act_window">
        <field name="name">Лікарі</field>
        <field name="res_model">res.users</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('is_doctor', '=', True)]</field>
    </record>
</odoo>
